FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      setFieldsDict;
}

defaultFieldValues
(
    volScalarFieldValue T 0
);

regions
(
    cylinderToCell
    {
        p1 (1 1 0);
        p2 (1 1 1);
        radius 0.5;

        fieldValues
        (
            volScalarFieldValue T 1
            //TODO "0.5 - 0.5 * tanh(((x - 1)*(x - 1) + (y - 1)*(y - 1) - 0.5) / 0.1)"
        );
    }
);
