- Archive `precice-*-iterations.log` files into `iterations-logs/` and compare them by SHA-256 hash against reference sidecar (`.iterations-hashes.json`) for implicit-coupling regression checks (fixes #440).
