- Fixed `Systemtest.py` so each Docker service receives its own build arguments from `components.yaml` (e.g. per-component `PLATFORM` and `PRECICE_REF`). [#907](https://github.com/precice/tutorials/pull/907)
