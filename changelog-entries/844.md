- `generate_reference_results.py` accepts `--build_args` to override values from `reference_versions.yaml` (e.g. `TUTORIALS_REF` on a feature branch). The generate-reference-results workflow uses `reference_versions.yaml` by default and can optionally use the workflow branch ([#869](https://github.com/precice/tutorials/pull/869)).
