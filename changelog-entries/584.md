- Fix Python bindings version being ignored in system tests: permanently activate `/home/precice/venv` in the `python_bindings` Docker stage via `VIRTUAL_ENV` and `PATH` env vars, and set `PRECICE_TUTORIALS_NO_VENV=1` in `python-bindings` and `fenics-adapter` component templates so tutorial run scripts skip creating their own venv and use the version installed by the Docker image (fixes #584).
