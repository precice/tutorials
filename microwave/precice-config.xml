<?xml version="1.0"?>
<precice-configuration>
  <log>
    <sink
      filter="%Severity% > debug and %Rank% = 0"
      format="---[precice] %ColorizedSeverity% %Message%"
      enabled="true" />
  </log>

  <solver-interface dimensions="2">
    <data:vector name="Velocity" />

    <mesh name="Fluid-Mesh">
      <use-data name="Velocity" />
    </mesh>

    <mesh name="Temperature-Mesh">
      <use-data name="Velocity" />
    </mesh>

    <participant name="Fluid">
      <use-mesh name="Fluid-Mesh" provide="yes" />
      <use-mesh name="Temperature-Mesh" from="Heat" />
      <export:vtu directory="preCICE-output-Fluid" />
      <mapping:nearest-neighbor
        direction="read"
        from="Temperature-Mesh"
        to="Fluid-Mesh"
        constraint="consistent" />
      <read-data name="Velocity" mesh="Fluid-Mesh" />
    </participant>

    <participant name="Heat">
      <use-mesh name="Temperature-Mesh" provide="yes" />
      <export:vtu directory="preCICE-output-Heat" />
      <write-data name="Velocity" mesh="Temperature-Mesh" />
    </participant>

    <m2n:sockets from="Heat" to="Fluid" exchange-directory=".." />

    <coupling-scheme:serial-explicit>
      <time-window-size value="0.005" />
      <max-time value="4.0" />
      <participants first="Heat" second="Fluid" />
      <exchange data="Velocity" mesh="Temperature-Mesh" from="Heat" to="Fluid" />
    </coupling-scheme:serial-explicit>
  </solver-interface>
</precice-configuration>
