FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      preciceDict;
}

preciceConfig "../precice-config.xml";

participant Neumann;

modules (CHT);

interfaces
{
  Interface1
  {
    mesh              Neumann-Mesh;
    patches           (interface);
    
    readData
    (
      Heat-Flux
    );
    
    writeData
    (
      Temperature
    );
  };
};

CHT
{
   k   [ 1  1 -3 -1 0 0 0 ] 3;
   solverType "basic";
};
