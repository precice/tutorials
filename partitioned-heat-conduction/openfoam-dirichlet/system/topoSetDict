FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}

actions
(
    {
        name        heatSourceSet;
        type        faceSet;
        action      new;
        source      boxToFace;
        box (0 0 0.01) (0.06 0.06 0.06);
    }
);
