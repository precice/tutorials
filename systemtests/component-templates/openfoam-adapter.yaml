image: "ghcr.io/precice/openfoam-adapter:{{ params["openfoam-adapter-ref"] }}"
user: ${MY_UID}:${MY_GID}
depends_on:    
  prepare:
    condition: service_completed_successfully
volumes:
  - /etc/passwd:/etc/passwd:ro #TODO: Maybe move those into a more general section ? 
  - /etc/group:/etc/group:ro
  - {{ tutorial_path }}:/tutorial
command: >
  /bin/bash -c "id && 
  cd /tutorial/{{ folder }} &&
  {{ params["openfoam-exectuable"] }} {{ run }} | tee {{ case_name }}.log 2>&1"